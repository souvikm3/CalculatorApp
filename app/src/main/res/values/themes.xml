<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalculatorMaking" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="operator" parent="commonButton">
        <item name="android:textColor">@color/btnBackground2</item>
    </style>
    <style name="digitButton" parent="commonButton">
        <item name="background">@color/black</item>

    </style>
<style name="commonButton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
    <item name="android:layout_width">72dp</item>
    <item name="android:layout_height">72dp</item>
    <item name="cornerRadius">36dp</item>
    <item name="android:textColor">@color/black</item>
<!--    <item name="android:textColor">@color/white</item>-->
    <item name="backgroundTint">@color/white</item>
    <item name="android:textSize">32dp</item>
    <item name="android:layout_margin">12dp</item>

</style>
    <style name="Theme.CalculatorMaking" parent="Base.Theme.CalculatorMaking" />
</resources>